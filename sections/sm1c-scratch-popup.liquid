{%- liquid
  if cart.item_count >= 3
    assign sm1c_prize_pct = 20
    assign sm1c_prize_code = section.settings.code_20
  elsif cart.item_count == 2
    assign sm1c_prize_pct = 15
    assign sm1c_prize_code = section.settings.code_15
  else
    assign sm1c_prize_pct = 10
    assign sm1c_prize_code = section.settings.code_10
  endif
-%}
{%- style -%}
#sm1c-scratch-{{ section.id }} {
  position: fixed;
  inset: 0;
  z-index: 9999;
  background: rgba(20,14,10,.7);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
  font-family: 'Work Sans', sans-serif;
  opacity: 0;
  pointer-events: none;
  transition: opacity .25s ease;
}
#sm1c-scratch-{{ section.id }}.is-visible {
  opacity: 1;
  pointer-events: auto;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-card {
  position: relative;
  width: 100%;
  max-width: 100%;
  height: 100vh;
  min-height: 100vh;
  background: #fff;
  border-radius: 0;
  overflow: hidden;
  box-shadow: none;
  transform: scale(.97);
  transition: transform .3s ease;
}
#sm1c-scratch-{{ section.id }}.is-visible .sm1c-sp-card {
  transform: scale(1);
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-track {
  display: grid;
  grid-template-columns: 44% 56%;
  width: 100%;
  height: 100%;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-close {
  position: absolute;
  top: 28px;
  left: 32px;
  z-index: 3;
  font-size: 26px;
  line-height: 1;
  cursor: pointer;
  background: none;
  border: none;
  color: #111;
  padding: 4px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-left {
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 56px 64px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-logo {
  width: auto;
  height: 40px;
  max-width: 160px;
  object-fit: contain;
  flex: none;
  margin-bottom: 24px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-title {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 56px;
  font-weight: 400;
  line-height: 1.05;
  max-width: 560px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-sub {
  font-size: 16px;
  color: rgba(58, 46, 39, .8);
  margin-top: 14px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-cardwrap {
  position: relative;
  width: 100%;
  max-width: 440px;
  height: 220px;
  border-radius: 20px;
  margin-top: 40px;
  overflow: hidden;
  box-shadow: 0 20px 50px rgba(0,0,0,.18);
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  background: oklch(97% 0.02 30);
  text-align: center;
  padding: 12px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize-pct {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 28px;
  font-weight: 400;
  color: #E8637E;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize-code {
  font-family: ui-monospace, monospace;
  font-size: 15px;
  font-weight: 700;
  background: #fff;
  border: 1.5px dashed #E8637E;
  padding: 6px 14px;
  border-radius: 8px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize-actions {
  display: flex;
  gap: 8px;
  margin-top: 4px;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize-actions button,
#sm1c-scratch-{{ section.id }} .sm1c-sp-prize-actions a {
  font-size: 11px;
  font-weight: 700;
  padding: 8px 14px;
  border-radius: 100px;
  border: none;
  cursor: pointer;
  text-decoration: none;
  font-family: inherit;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-copy {
  background: #fff;
  border: 1.5px solid #111 !important;
  color: #111;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-apply {
  background: #111;
  color: #fff;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  cursor: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='36' height='36'><circle cx='18' cy='18' r='14' fill='%23f1d38a' stroke='%237a5a1e' stroke-width='2'/><path d='M11 18h14M18 11v14' stroke='%237a5a1e' stroke-width='1.4' stroke-linecap='round' opacity='.55'/></svg>") 18 18, pointer;
  touch-action: none;
  transition: opacity .5s ease;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-canvas.is-cleared {
  opacity: 0;
  pointer-events: none;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-cardwrap::after {
  content: '';
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: linear-gradient(115deg, transparent 30%, rgba(255, 255, 255, .5) 45%, transparent 60%);
  background-size: 250% 250%;
  background-position: -60% -60%;
  animation: sm1c-sp-shimmer 3.2s ease-in-out infinite;
  mix-blend-mode: overlay;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-cardwrap:has(.sm1c-sp-canvas.is-cleared)::after {
  display: none;
}
@keyframes sm1c-sp-shimmer {
  0% { background-position: -60% -60%; }
  100% { background-position: 160% 160%; }
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-right {
  position: relative;
  background: #eee;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-right img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  display: block;
}
#sm1c-scratch-{{ section.id }} .sm1c-sp-swipe-hint {
  display: none;
}
@media (max-width: 1024px) {
  #sm1c-scratch-{{ section.id }} .sm1c-sp-track {
    display: flex;
    flex-direction: row;
    width: 100%;
    height: 100%;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-track::-webkit-scrollbar {
    display: none;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-left,
  #sm1c-scratch-{{ section.id }} .sm1c-sp-right {
    flex: 0 0 100%;
    width: 100%;
    height: 100%;
    scroll-snap-align: start;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-left {
    position: relative;
    padding: 56px 24px 40px;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-title {
    font-size: 32px;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-swipe-hint {
    display: flex;
    position: absolute;
    left: 0;
    right: 0;
    bottom: 20px;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-size: 12px;
    font-weight: 600;
    color: #3A2E27;
    opacity: .7;
  }
  #sm1c-scratch-{{ section.id }} .sm1c-sp-swipe-hint svg {
    animation: sm1c-sp-hint-nudge 1.6s ease-in-out infinite;
  }
  @keyframes sm1c-sp-hint-nudge {
    0%, 100% { transform: translateX(0); }
    50% { transform: translateX(6px); }
  }
}
{%- endstyle -%}

<div id="sm1c-scratch-{{ section.id }}" data-sm1c-scratch-popup hidden>
  <div class="sm1c-sp-card">
    <button type="button" class="sm1c-sp-close" data-sm1c-close aria-label="Close">✕</button>
    <div class="sm1c-sp-track">
      <div class="sm1c-sp-left">
        {%- if section.settings.logo != blank -%}
          <img class="sm1c-sp-logo" src="{{ section.settings.logo | image_url: width: 260 }}" alt="">
        {%- endif -%}
        <div class="sm1c-sp-title">{{ section.settings.title }}</div>
        <div class="sm1c-sp-sub">{{ section.settings.subtitle }}</div>

        <div class="sm1c-sp-cardwrap">
          <div class="sm1c-sp-prize">
            <div class="sm1c-sp-prize-pct">{{ sm1c_prize_pct }}% OFF</div>
            <div class="sm1c-sp-prize-code">{{ sm1c_prize_code }}</div>
            <div class="sm1c-sp-prize-actions">
              <button type="button" class="sm1c-sp-copy" data-sm1c-copy data-code="{{ sm1c_prize_code }}">Copy code</button>
              <a class="sm1c-sp-apply" href="/discount/{{ sm1c_prize_code }}?redirect=%2Fcollections%2Fall">Apply &amp; Shop</a>
            </div>
          </div>
          <canvas class="sm1c-sp-canvas" data-sm1c-canvas></canvas>
        </div>

        {%- if section.settings.photo != blank -%}
          <div class="sm1c-sp-swipe-hint">
            <span>Swipe to see</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14M13 6l6 6-6 6"/>
            </svg>
          </div>
        {%- endif -%}
      </div>
      <div class="sm1c-sp-right">
        {%- if section.settings.photo != blank -%}
          {{ section.settings.photo | image_url: width: 900 | image_tag: loading: 'lazy', alt: '' }}
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

<script>
(function () {
  var root = document.getElementById('sm1c-scratch-{{ section.id }}');
  if (!root) return;
  var LAST_SHOWN_KEY = 'sm1c_scratch_last_shown';
  var DELAY_MS = {{ section.settings.delay_seconds | default: 1.5 }} * 1000;
  var THRESHOLD = {{ section.settings.scratch_threshold | default: 55 }};
  var REPEAT_MS = {{ section.settings.repeat_minutes | default: 2 }} * 60 * 1000;

  function markShown() {
    try { localStorage.setItem(LAST_SHOWN_KEY, String(Date.now())); } catch (e) {}
  }

  function hidePopup() {
    root.classList.remove('is-visible');
    setTimeout(function () { root.hidden = true; }, 250);
  }

  function showPopup() {
    root.hidden = false;
    markShown();
    requestAnimationFrame(function () {
      root.classList.add('is-visible');
    });
  }

  var closeBtn = root.querySelector('[data-sm1c-close]');
  if (closeBtn) closeBtn.addEventListener('click', hidePopup);
  root.addEventListener('click', function (e) {
    if (e.target === root) hidePopup();
  });

  var copyBtn = root.querySelector('[data-sm1c-copy]');
  if (copyBtn) {
    copyBtn.addEventListener('click', function () {
      var code = copyBtn.getAttribute('data-code');
      if (navigator.clipboard) {
        navigator.clipboard.writeText(code).then(function () {
          copyBtn.textContent = 'Copied!';
          setTimeout(function () { copyBtn.textContent = 'Copy code'; }, 1500);
        });
      }
    });
  }

  var canvas = root.querySelector('[data-sm1c-canvas]');
  if (canvas) {
    var ctx = canvas.getContext('2d');
    var cleared = false;

    function resizeCanvas() {
      var rect = canvas.getBoundingClientRect();
      canvas.width = rect.width;
      canvas.height = rect.height;
      drawScratchLayer();
    }

    function drawScratchLayer() {
      var w = canvas.width, h = canvas.height;

      var grad = ctx.createLinearGradient(0, 0, w, h);
      grad.addColorStop(0, '#f3d688');
      grad.addColorStop(0.5, '#d9a83f');
      grad.addColorStop(1, '#e9c15a');
      ctx.globalCompositeOperation = 'source-over';
      ctx.fillStyle = grad;
      ctx.fillRect(0, 0, w, h);

      var vignette = ctx.createRadialGradient(w / 2, h / 2, Math.min(w, h) * 0.2, w / 2, h / 2, Math.max(w, h) * 0.75);
      vignette.addColorStop(0, 'rgba(0,0,0,0)');
      vignette.addColorStop(1, 'rgba(90,60,10,.35)');
      ctx.fillStyle = vignette;
      ctx.fillRect(0, 0, w, h);

      var grainCount = Math.min((w * h) / 90, 6000);
      for (var n = 0; n < grainCount; n++) {
        var nx = Math.random() * w;
        var ny = Math.random() * h;
        ctx.fillStyle = Math.random() > 0.5 ? 'rgba(255,255,255,.12)' : 'rgba(0,0,0,.08)';
        ctx.fillRect(nx, ny, 1, 1);
      }

      var streaks = [
        { x: w * 0.16, y: -h * 0.1, len: h * 1.3, width: 12, rot: -0.42, a: .32 },
        { x: w * 0.34, y: -h * 0.2, len: h * 1.4, width: 20, rot: -0.36, a: .26 },
        { x: w * 0.55, y: -h * 0.15, len: h * 1.35, width: 9, rot: -0.46, a: .3 },
        { x: w * 0.72, y: -h * 0.25, len: h * 1.45, width: 16, rot: -0.38, a: .28 }
      ];
      streaks.forEach(function (s) {
        ctx.save();
        ctx.translate(s.x, s.y);
        ctx.rotate(s.rot);
        ctx.strokeStyle = 'rgba(255,255,255,' + s.a + ')';
        ctx.lineWidth = s.width;
        ctx.lineCap = 'round';
        ctx.beginPath();
        ctx.moveTo(0, 0);
        ctx.lineTo(0, s.len);
        ctx.stroke();
        ctx.restore();
      });

      ctx.fillStyle = 'rgba(255,255,255,.9)';
      ctx.font = '800 17px "Bricolage Grotesque", sans-serif';
      ctx.textAlign = 'center';
      ctx.textBaseline = 'middle';
      ctx.shadowColor = 'rgba(90,60,10,.4)';
      ctx.shadowBlur = 5;
      ctx.fillText('SCRATCH HERE', w / 2, h / 2);
      ctx.shadowBlur = 0;
    }

    var BRUSH_RADIUS = 26;

    function eraseDot(x, y) {
      ctx.globalCompositeOperation = 'destination-out';
      ctx.beginPath();
      ctx.arc(x, y, BRUSH_RADIUS, 0, Math.PI * 2);
      ctx.fill();
    }

    function eraseStroke(x0, y0, x1, y1) {
      ctx.globalCompositeOperation = 'destination-out';
      ctx.lineCap = 'round';
      ctx.lineJoin = 'round';
      ctx.lineWidth = BRUSH_RADIUS * 2;
      ctx.beginPath();
      ctx.moveTo(x0, y0);
      ctx.lineTo(x1, y1);
      ctx.stroke();
      eraseDot(x1, y1);
    }

    function getScratchedPercent() {
      var w = canvas.width, h = canvas.height;
      if (!w || !h) return 0;
      var data;
      try {
        data = ctx.getImageData(0, 0, w, h).data;
      } catch (e) {
        return 0;
      }
      var total = 0, cleared_px = 0;
      for (var i = 3; i < data.length; i += 4 * 6) {
        total++;
        if (data[i] < 40) cleared_px++;
      }
      return total ? (cleared_px / total) * 100 : 0;
    }

    function revealPrize() {
      if (cleared) return;
      cleared = true;
      canvas.classList.add('is-cleared');
    }

    var isDown = false;
    var lastPos = null;
    var percentCheckPending = false;

    function pointerPos(evt) {
      var rect = canvas.getBoundingClientRect();
      var point = evt.touches ? evt.touches[0] : evt;
      return { x: point.clientX - rect.left, y: point.clientY - rect.top };
    }

    function checkThreshold() {
      if (percentCheckPending || cleared) return;
      percentCheckPending = true;
      requestAnimationFrame(function () {
        percentCheckPending = false;
        if (getScratchedPercent() >= THRESHOLD) revealPrize();
      });
    }

    function handleStart(evt) {
      if (cleared) return;
      evt.preventDefault();
      isDown = true;
      lastPos = pointerPos(evt);
      eraseDot(lastPos.x, lastPos.y);
      checkThreshold();
    }

    function handleMove(evt) {
      if (!isDown || cleared) return;
      evt.preventDefault();
      var p = pointerPos(evt);
      eraseStroke(lastPos.x, lastPos.y, p.x, p.y);
      lastPos = p;
      checkThreshold();
    }

    function handleEnd() {
      isDown = false;
      lastPos = null;
    }

    canvas.addEventListener('mousedown', handleStart);
    canvas.addEventListener('mousemove', handleMove);
    window.addEventListener('mouseup', handleEnd);
    canvas.addEventListener('touchstart', handleStart, { passive: false });
    canvas.addEventListener('touchmove', handleMove, { passive: false });
    canvas.addEventListener('touchend', handleEnd);
    canvas.addEventListener('touchcancel', handleEnd);

    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();
  }

  var lastShown = 0;
  try { lastShown = parseInt(localStorage.getItem(LAST_SHOWN_KEY), 10) || 0; } catch (e) {}
  var dueAgainAt = lastShown + REPEAT_MS;
  if (Date.now() >= dueAgainAt) {
    setTimeout(showPopup, DELAY_MS);
  }
})();
</script>

{% schema %}
{
  "name": "SM1C Scratch to Win",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "text", "id": "title", "label": "Headline", "default": "Try your luck" },
    { "type": "text", "id": "subtitle", "label": "Subheading", "default": "Scratch below to see what you win" },
    { "type": "image_picker", "id": "photo", "label": "Campaign photo" },
    { "type": "text", "id": "code_10", "label": "Discount code — 1 item in cart", "default": "SCRATCH10" },
    { "type": "text", "id": "code_15", "label": "Discount code — 2 items in cart", "default": "SCRATCH15" },
    { "type": "text", "id": "code_20", "label": "Discount code — 3+ items in cart", "default": "SCRATCH20" },
    { "type": "range", "id": "delay_seconds", "label": "Delay before showing (seconds)", "min": 0, "max": 10, "default": 4 },
    { "type": "range", "id": "scratch_threshold", "label": "Scratch % required to reveal", "min": 20, "max": 90, "step": 5, "default": 55 },
    { "type": "range", "id": "repeat_minutes", "label": "Show again after (minutes)", "info": "Popup reappears on a later page view once this many minutes have passed since it was last shown.", "min": 1, "max": 60, "default": 4 }
  ],
  "presets": [
    { "name": "SM1C Scratch to Win" }
  ]
}
{% endschema %}
